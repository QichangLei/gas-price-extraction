# if "one application" ask for private key, it may be github private key, #you should check it on the github website 
cd /media/qichang/Data/research/Gas_price
conda activate gas_price_cv

Run it:

bash./run_batch.sh /data/price_1/

Or with output directory:
./run_batch.sh /data/price_1/ ./results
